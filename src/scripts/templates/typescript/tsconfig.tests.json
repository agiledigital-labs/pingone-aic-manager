{
  "//": "The test program. Separate from tsconfig.json because the tests run under node — they need `node:test` and a modern lib, while the endpoint program is deliberately pinned to the narrow IDM runtime lib. Managed file — regenerated by `aic workspace update`.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "types": ["node"]
  },
  "include": ["framework/**/*", "src/**/*", "tests/**/*"],
  "exclude": ["node_modules", "dist", "openapi"]
}

# Local notes — pingone-aic-manager

Machine-specific paths, tenants, and tooling. **Gitignored — never commit this
file.** Copy to `.ai/local.md` and fill in.

**No secrets.** Record _where_ a credential comes from, never its value. The
vault master password lives in `.envrc`; the service-account JWK and log API
keys live encrypted in `.aic/`. Nothing belongs here but paths and hostnames.

## Tenant

The tenant list lives in `.aic/config.toml` (gitignored, created by
`aic ctx add`). Note here which tenant you normally work against and anything
surprising about it.

- Default context: `<name>` — `https://<tenant-host>` (sandbox / prod?)

## Sibling checkouts

`docs/api/*.md` cite evidence from checkouts by `~/w/…` path. Record which of
them actually exist here; mark the rest missing so an agent stops looking.

**A client checkout's path belongs here and nowhere else** — not in a committed
doc, not even with the client's name swapped for a placeholder. The path shape
alone announces that a client corpus exists and roughly what is in it, which is
the thing worth not publishing; the committed docs cite the evidence and defer
to this file for where it lives.

- `<repo>`: `/path/to/<repo>` — <why you'd open it>
- `<repo>`: **not present on this machine** — cited in `docs/api/<file>.md`

## Local tooling

- <anything installed outside the repo that the workflow assumes>

## Sensitive-metadata denylist

`scripts/check-sensitive-metadata.sh` detects by shape. To also reject literal
client names, put one regex per line in a file **outside the repo** (or in
`.ai/denylist.txt`, which is gitignored) and export its path:

```sh
export SENSITIVE_DENYLIST="$HOME/.config/aic/denylist.txt"
```

Use word boundaries. A bare three-letter abbreviation can match inside ordinary
camelCase identifiers, and a hook that cries wolf gets bypassed.
